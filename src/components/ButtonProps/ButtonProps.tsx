import React from 'react'
import './ButtonProps.css'; //Подключаем файл ButtonProps.css

const ButtonProps = () => {
  return (
    <button className = 'newClick'>Click Me</button>
  )
}

export default ButtonProps